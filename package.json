{
  "dependencies": {
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.1"
  },
   "scripts": {
    "tests": "npx playwright test",
    "test:specific": "npx playwright test --grep sep-login.spec.js",
    "test:tag": "npx playwright test --grep @sep",
    "report": "npx playwright show-report"
  }
}
